# Dependencies
node_modules/
yarn.lock
# package-lock.json is intentionally tracked for reproducible CI builds
# (was previously ignored; keep lockfile committed)
yarn.lock

# Build output
dist/
.vite/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Legacy files (remove after migration)
# legacy/
#
# Exclude Arduino local package caches and other large local SDK dirs (Windows)
AppData/Local/Arduino15/
AppData/Local/Arduino15/**

# Exclude any deeply nested SDK directories that cause long path errors on Windows
**/AppData/Local/Arduino15/**

# Temporary build artifacts
build/
out/
